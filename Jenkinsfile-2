pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook --inventory inventory/ansible-jenkins/hosts ansible-jenkins-playbook.yml'
      }
    }
  }
}